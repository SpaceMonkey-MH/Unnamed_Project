class_name EnemyClass
extends CharacterClass

# The player as a variable, so that it can be used by the states. Might have to assign that in the level scene.
@export var player : PlayerClass

#func _ready():
#	print(move_speed)

